Native select with the system's chevron. Use for closed domain enums — category, difficulty, InterviewRound, target market.

```jsx
<Select options={[{value:'Theory',label:'Theory'},{value:'LeetCode',label:'LeetCode'}]} />
```
