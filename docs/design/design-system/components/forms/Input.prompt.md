Single-line text control, 40px tall, 3px radius, `--border-strong` edge (the decorative hairline is too light to meet the 3:1 UI-contrast rule).

```jsx
<Input id="email" type="email" placeholder="you@example.com" />
```
