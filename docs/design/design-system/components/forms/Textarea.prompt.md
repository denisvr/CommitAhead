Multi-line control for Markdown fields (approach notes, STAR answers, summaries) and pasted job text.

```jsx
<Textarea rows={10} mono placeholder="Paste the job posting" />
```

Use `mono` wherever the user is editing Markdown or raw source.
