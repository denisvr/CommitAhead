A square, label-less action for row-level affordances (edit, delete, dismiss).

```jsx
<IconButton icon="pencil" label="Edit item" />
<IconButton icon="trash-2" label="Delete item" tone="danger" />
```

`label` is mandatory — it is the accessible name. Never use one where a text button fits.
