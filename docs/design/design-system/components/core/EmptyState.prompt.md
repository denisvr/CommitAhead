Shown when a list has no rows yet. Type and space only.

```jsx
<EmptyState title="No study items yet" action={<Button icon="plus">New study item</Button>}>
  Add one, or analyse a job posting and accept the proposals it produces.
</EmptyState>
```

No illustration, no icon, no emoji — the brand has no imagery.
