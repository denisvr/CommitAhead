Severity and status marker — the only place colour carries meaning.

```jsx
<Badge tone="critical">High gap</Badge>
<Badge tone="draft">Draft — needs your review</Badge>
```

Always pair the colour with a word so the meaning survives greyscale. `draft` is reserved for AI output that has not been confirmed.
