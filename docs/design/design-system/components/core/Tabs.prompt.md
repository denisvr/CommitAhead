Section switch inside one screen — used by the CVPresentation editor and the typed StudyItem detail.

```jsx
<Tabs value={tab} onChange={setTab} items={[{value:'content',label:'Content'},{value:'preview',label:'Preview'}]} />
```

The active underline is the brand's 3px rule motif — the same one under the wordmark.
