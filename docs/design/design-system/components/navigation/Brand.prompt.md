The wordmark as used inside the product: the outlined bookmark symbol plus live Public Sans type.

```jsx
<Brand size={17} />
<Brand size={30} />
```

For print, export and anything outside the app, use the outlined lockup files in `assets/logo/bookmark/` instead.
