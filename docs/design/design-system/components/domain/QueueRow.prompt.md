One StudyItem in the ranked queue. Rows, not cards — separated by a single hairline.

```jsx
<QueueRow rank="02" title="Design a Rate Limiter" category="System Design" score={88}
  meta="Linked from Ledgerline — Senior Backend · reviewed 6 days ago" />
```

Pass `dense` inside a `data-density="dense"` table region. `meta` should always state a reason, never restate the title.
