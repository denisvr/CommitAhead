Shows *why* an item ranks where it does, by splitting EffectiveScore into its three inputs at 100%, 62% and 32% accent opacity.

```jsx
<ScoreBreakdown importance={37} demand={32} masteryGap={23} />
<ScoreBreakdown variant="bar" importance={37} demand={32} masteryGap={23} />
```

This is the only chart in the product, and it exists because the ranking must explain itself. Do not add others.
